# Follow-ups ledger（append-only · 永不删行 · 提交进仓库）

> pilot 的 review triage 把「真问题但不阻塞（B）」和延后项记在这里。
> 主线 task 全部完成后，由 `pilot run` 批量合成一个 cleanup PR 做掉，逐条标 [x] done=PR#n。
> `- [ ]`=OPEN，`- [x]`=DONE。GitHub PR comment 是永久兜底。

- [x] FU-1 · B · src=T1.1.1 · 2026-08-20 · .pilot.yml 的 base_branch/integration_branch 行尾内联注释会让 grade-change.sh 等脚本的默认 base 解析失败（把注释也读进去），需要把注释挪到单独一行或改用脚本能正确 strip 的格式 · done=PR#34
- [x] FU-2 · B · src=T1.1.2 review (Codex, production-failure-mode lens) · 2026-08-20 · scripts/publish.sh 和已废弃的 scripts/auto-publish.sh 用 pnpm build 2>&1 | tail -3 且没开 pipefail，set -e 抓不住 pnpm build 失败（tail 的退出码掩盖了它）。T1.1.2 加的 postbuild pagefind 索引步骤是刻意 fail-closed 的，会让这个既有 bug 被触发的概率变高。当前实际在用的 scripts/publish-blog.sh 已经是 set -euo pipefail，不受影响；scripts/publish.sh 需要补 pipefail 或改成不走 tail 管道执行 pnpm build · done=PR#34
- [x] FU-3 · B · src=T1.1.3 · 2026-08-21 · search.astro 用的是 Pagefind 的 Default UI(pagefind-ui.js/css)；build 时 pagefind 提示 1.5.0 起新集成建议用 Component/Modular UI(更好的可访问性和自定义能力)。Default UI 官方说明仍会继续支持,不阻塞,等 Phase 0B/1 决定要不要长期投入这个功能时再考虑迁移 · done=PR#34
- [ ] FU-4 · B · src=review PR#38 · 2026-08-22 · ColBERT/late-interaction (sentence-transformers v6) 评估：现阶段不接入，理由见对话记录——RRF 混合已覆盖精确技术名词场景，且 Cloudflare Workers AI/Vectorize 均不原生支持多向量 MaxSim，接入需自建推理。若 F1.3 上线后精确技术名词查询仍有明显失败案例，可重新评估文章提到的'两阶段最小接入'（现有召回不动，只对 top50 候选做 MaxSim rerank）
- [ ] FU-5 · B · src=review PR#38 · 2026-08-22 · baseline-results.md 的查询编号(#12-#17)与 queries.md/vector-comparison-report.md 不一致，且自相矛盾（第16行标'3D可视化工具'但散文写#16是recursive self improvement）。不影响本次裁定（裁定按名字引用，未用编号交叉引用），但会误导以后按编号查阅的人
- [ ] FU-6 · B · src=review PR#38 · 2026-08-22 · T1.3.6 登录限速用 KV 计数器（15分钟5次/IP）对分布式撞库较弱，因为 Cloudflare KV 最终一致（全球传播约60s）。严重度低（单一共享密码+低访问量场景），后续如需加强可换 Durable Objects 或 CF 原生 rate limiting binding
- [ ] FU-7 · B · src=T1.3.1 self-review (security lens) · 2026-08-22 · T1.3.1 索引脚本复用 CLOUDFLARE_REGISTRAR_TOKEN(另一项目cmic长期在用的凭据)做 Vectorize create/upsert 这类真实写操作，权限范围可能比这个用途需要的更宽。值不值得为 T1.3.x 铸造一个 Workers AI:Edit + Vectorize:Edit 专用窄权限 token，是账号级操作需要用户决定，非阻塞（2026-08-22 更新：用户已铸造新 token 并补齐两条权限，当前在用，遗留问题只剩旧 token 要不要收回）
- [ ] FU-8 · B · src=T1.3.1 self-review (production-failure-mode lens) · 2026-08-22 · build-vectorize-index.py 不做 spec.md 要求的'先记录旧chunk_id再删'增量更新流程——两次运行之间若文章内容被编辑，旧 chunk_id 不会被清理，留下孤儿向量。当前只在脚本文档里提示手动用 wrangler vectorize delete-by-ids 清理，正式方案交给 T1.4.1 的对账逻辑
- [ ] FU-9 · B · src=T1.3.2 self-review · 2026-08-22 · chunking.py 在硬上限16片和目标300-600 token两个约束无法同时满足的极长文章上，最后一片会残留超量（全库478篇里2篇，最高1348 token，约为上限的2.2倍）。根因是贪心分块+按block为最小单位，无法在block内部再切分。如果后续发现这影响检索质量，可以考虑对超大block做二次细分，当前先接受这个已知残留
- [ ] FU-10 · B · src=T1.3.2 self-review · 2026-08-22 · tasks.md/spec.md/architecture.md都没有明确'每篇12-16 chunk'预算是按整篇还是按每种语言各自算，chunking.py当前实现按每语言处理（双语文章理论上限32）。全库只有3篇文章总chunk数超过16（最多24）。如果后续判断应该是'每篇'总量，需要重新调整MAX_CHUNKS_PER_LANGUAGE的语义
- [ ] FU-11 · B · src=review PR#48 (R1b + Codex) · 2026-08-22 · rate-limit.js 的 KV read-modify-write（get 后 put）不是原子操作，同一 IP 并发请求可能互相读到旧计数、绕过限速——这跟 FU-6 记的'跨 PoP 最终一致性弱'是两个不同机制（FU-6 是分布式一致性延迟，这条是单个 PoP 内并发请求的竞态），应该分开记录。真正修复需要 Durable Objects 或 Cloudflare 原生 rate limiting binding（跟 FU-6 的建议方案一致），当前单一共享密码+低访问量场景下可接受
- [x] FU-12 · B · src=T1.3.3 self-review (production-failure-mode lens) · 2026-08-22 · functions/api/search-auth.js（T1.3.6，已合并）只信 Content-Length 请求头判断 body 是否超限，chunked encoding 或谎报 Content-Length 能绕过体积上限检查再走到 request.json() 解析——T1.3.3 的 search.js 用同样的修法（先读成文本量实际字节数）修过了，search-auth.js 还没有。严重度低（登录接口本身已有 MAX_PASSWORD_LENGTH 兜底实际损耗，且这条路径不涉及计费调用），非阻塞 · done=PR#55
- [ ] FU-13 · B · src=T1.3.3 self-review (production-failure-mode lens) · 2026-08-22 · /api/search 上线后 Workers AI + Vectorize 第一次在这个仓库有了实际计费调用量；限速本身的 KV 最终一致性弱点（见 FU-6/FU-11）在计费场景下风险等级更高。建议在 Cloudflare 后台给这两项服务配置用量告警，作为限速之外的兜底，不是代码能解决的问题，需要人工在 dashboard 操作
- [x] FU-14 · A · src=T1.3.3 post-merge deploy verification · 2026-08-22 · GitHub Actions 的 CLOUDFLARE_API_TOKEN secret 已失效（.github/workflows/deploy.yml 的 Deploy 步骤报 'Not logged in'），至少从 2026-08-22T10:34 起（PR#52 合并触发的那次部署也失败，run 32572408148）自动部署已经在静默失败——push 到 main 之后 CI 显示红叉但没有任何人工介入去查。已用本地 wrangler token（blog/.env 里那份，跟坏掉的 GitHub secret 不是同一份）手动部署过 T1.3.3 验证用，生产环境暂时是最新的。原建议是让用户去 GitHub repo Settings → Secrets 更新这个 token，但 PR#54（chore/manual-deploy-only）把用这个 secret 的 workflow 整个删掉了，改成部署完全走本地 wrangler——这个 secret 从此不再被任何东西读取，"去更新它"这条建议本身连同它所属的 workflow 一起作废，不需要用户采取任何行动 · done=PR#54
- [ ] FU-15 · B · src=chore/manual-deploy-only self-review (security lens) · 2026-08-22 · scripts/update-analytics.sh 默认分支（没设 NODE_EXTRA_CA_CERTS/CF_CA_CERT 时）用 NODE_TLS_REJECT_UNAUTHORIZED=0 跑 wrangler deploy，关掉了传输 CLOUDFLARE_API_TOKEN 这次请求的证书校验，有 MITM 风险。**round 2 review 已核实这是本仓库有意的既定模式**（`scripts/publish-blog.sh`、`.agents/skills/blog-publisher/SKILL.md:297-300` 都用同一个"有 CF_CA_CERT/NODE_EXTRA_CA_CERTS 就用真 CA、没有就退化到 TLS bypass"的写法，SKILL.md 明确写"token + TLS workaround required in this environment"），不是未解释的遗留代码，本地网络环境目前需要它。降级为非阻塞的长期改进项：如果以后拿到了这个环境实际使用的代理/网络的真实 CA 证书，应该配置 CF_CA_CERT 从根上去掉这个 bypass；在那之前继续接受现状
- [x] FU-16 · B · src=T1.3.4 self-review (correctness lens) · 2026-08-22 · /api/search 缓存 key 的归一化只做了 trim+小写（query.trim().toLowerCase()），没有折叠内部多余空格、Unicode NFC/NFD、全角半角字符（常见于中文输入法）。语义相同的查询可能因为这些差异各算一份缓存，最坏情况只是多几次计费调用（缓存未命中），不是正确性 bug，但值得记录，以后如果发现缓存命中率明显偏低可以从这里查 · done=PR#57
- [ ] FU-17 · B · src=T1.3.4 self-review (correctness lens) · 2026-08-22 · /api/search 的查询结果缓存（6小时TTL）目前没有跟索引更新挂钩的失效机制。当前 T1.4.1（发布流程接入增量索引 hook）还是 BACKLOG，索引本身的新鲜度上限本来就比6小时粗，所以现在不构成额外损失；但 T1.4.1 上线后，这个缓存会成为'重复搜同一个词'这条窄路径上新内容可见的新增延迟来源，需要在做 T1.4.1 时一并考虑是否要在重新索引时清一次 searchcache: 前缀的 key（或者缩短TTL/改成按索引版本号做 key 的一部分）
- [ ] FU-18 · B · src=T1.3.4 self-review (production-failure-mode lens) · 2026-08-22 · /api/search 缓存上线后，BLOG_SEARCH_KV 这个 namespace（同时扛着 T1.3.5 manifest 和 T1.3.6/T1.3.4 的限速计数器）每次缓存未命中都多一次 KV 写入，账号级/namespace 级的每日操作配额消耗速度比之前快（具体数字取决于 Cloudflare 套餐，未核实当前实际配额）。当前个人博客+单一共享密码的访问量级下影响有限，但跟 FU-13 建议的用量告警是同一个方向的问题，值得一起看
- [x] FU-19 · B · src=T1.3.4 self-review (production-failure-mode lens) · 2026-08-22 · functions/_lib/rate-limit.js 的 checkAndIncrement 对 KV 的 get/put 调用都没有 try/catch——KV 配额耗尽或短暂故障时会直接抛异常，未被上层捕获会导致一次本该成功的搜索/登录请求变成裸 500，而不是当前该走的降级路径。这是 T1.3.6 就有的既有代码（已经过多轮评审），本次 T1.3.4 没有改动它，只是新增的缓存写入让 KV 写入量变多、更容易撞到这个問題，非阻塞但值得单独修一次 · done=PR#57
- [x] FU-20 · B · src=review PR#52/#55/#57，PR#58 review 正式记入账本 · 2026-08-23 · search.astro:410（**订正**：不是 :409，那一行是同一个 Promise.all 里已经有 .catch 的 Pagefind 那支，:409 正好是用来对比的反例）的 searchVectorRanked 没有 .catch。函数内部只处理了 401 和 !resp.ok；fetch 本身 reject（断网/DNS/TLS）或 resp.json() 解析失败会穿透 Promise.all，让整轮搜索抛异常，连已经拿到的 Pagefind 结果也不渲染——正好是 T1.3.4 声称交付的"降级"属性的反面。这个缺陷此前在 #52/#55/#57 三轮评审里都提过但从未记入账本，导致 F1.3 收工时差点漏掉。**订正修法**：review 实测证实 `.catch(() => null)` 是空操作——`runCombinedSearch` 紧接着读 `vectorOutcome.expired`，传 null 只是把崩溃从 Promise.all 挪到下一行，可观察行为（整轮搜索仍然作废、Pagefind 结果仍然丢失）跟不修完全一样。真正的一行修复是 `.catch(() => ({ expired: false, results: [] }))`——已跑满 5 个场景验证（401 会话过期语义保留、503/fetch reject/非法 JSON 都能正确降级到"渲染 Pagefind 结果+0 条向量"、200 正常路径不受影响） · done=PR#58
- [ ] FU-24 · B · src=fix/nav-search-icon-only round 2 review (增量复审) · 2026-08-23 · .env.example 里 CLOUDFLARE_API_TOKEN/CLOUDFLARE_ACCOUNT_ID 用的是 `your_token_here`/`your_account_id_here` 占位符文案（跟文件里 WECHAT_APP_SECRET/GISCUS_REPO_ID/GISCUS_CATEGORY_ID 等既有字段是同一套约定，不是本次新引入的风格）。但 publish.sh/scan-sources.sh/publish-blog.sh/auto-publish.sh 这四个脚本会整体 `source .env`，如果有人复制 .env.example 为 .env 却忘了填这两行，会把占位符文本原样当成真实 token/account id 导出，Cloudflare 会返回一个看不懂的鉴权错误而不是"没配置"——非阻塞，值得以后统一把这套 .env.example 的占位符约定改成真正的空值或加一层"必须替换"的显式检查
- [ ] FU-25 · B · src=feat/search-usage-analytics security review · 2026-08-23 · /api/search 新增的搜索使用统计（写入 Analytics Engine 的 blog_search_events 数据集）存的是**原始** IP + **原始**（未哈希）归一化查询词，跟这个仓库自己的既有纪律不一致——T1.3.6 的会话限速哈希 Cookie、T1.3.4 的缓存 key 哈希查询词，都是刻意避免存明文；这次存明文是用户明确要求（"什么时间、哪个 IP、搜了什么"），不是疏漏，且这份数据只有登录后才能通过 /api/search-analytics.json 查到（见该文件头注释的缓存隔离设计）。遗留的开放问题：Analytics Engine 数据集本身的保留期由 Cloudflare 平台侧决定，代码里没有验证过、也没有代码层面的兜底（不像 KV 缓存有显式 TTL），如果平台默认保留期比预期长，暴露窗口比看起来大；且当前复用的是 CF_ANALYTICS_TOKEN（本来只读公开聚合 RUM 数据），代码已做成可选升级路径（CF_ANALYTICS_ENGINE_TOKEN 优先），但还没有真正铸造一个只读 Analytics Engine 权限的窄权限 token——这是账号级操作，需要用户去 Cloudflare Dashboard 做，非阻塞但建议尽快办
- [ ] FU-26 · B · src=feat/search-usage-analytics security review · 2026-08-23 · /api/search-analytics.json 端点代码层面正确地把响应标成 `cache-control: private, no-store`、且不使用 caches.default，但这只保证这个 Function 自己不主动缓存——没有核实过 Cloudflare 账号/zone 级别是否配置了会覆盖来源 Cache-Control 的 Cache Rule（例如对 /api/* 开了"Cache Everything"且忽略源站头）。如果确实存在这样的规则，这个端点返回的登录用户搜索统计仍有可能被边缘缓存住并泄露给后续访客——这是运维层面需要在 Cloudflare Dashboard 里核实的事，代码本身管不到
- [ ] FU-27 · B · src=feat/public-search-no-auth production-failure-mode review · 2026-08-23 · /api/search 去掉登录门禁后，唯一的滥用防线是按 IP 限速（30 次/5 分钟）——这只约束单个 IP，不约束总的独立访客量。之前密码墙下真实流量接近零，现在完全公开，如果这篇博客/搜索功能被分享到社交媒体引来一波真实访客，每个新 IP 都各自有一份 30 次/5 分钟的额度，聚合起来的 Workers AI/Vectorize 计费调用量、以及 BLOG_SEARCH_KV（同一个 namespace 还扛着缓存/manifest，见 FU-18）的操作配额消耗，都可能比密码墙时代高出不少。checkAndIncrement 对 KV 故障 fail-closed（FU-19 已修），意味着真撞上配额上限时是"所有人都 429"而不是裸崩溃或无限放行，失败模式本身是安全的，但会在最公开曝光的那一刻让搜索看起来"坏了"。跟 FU-13 建议的用量告警是同一个方向，值得一起在 Cloudflare 后台配置
